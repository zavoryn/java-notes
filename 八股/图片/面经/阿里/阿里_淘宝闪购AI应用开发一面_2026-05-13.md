# 淘宝闪购 AI应用开发 一面

> 来源：牛客，作者：Code_Agent
> 链接：https://www.nowcoder.com/discuss/884091787340419072
> 日期：2026-05-13
> 学校：广西大学，专业：算法工程师
> 备注：内容有60%在付费墙后，以下为可见部分

---

### 1. 自我介绍

### 2. Java 中 final、finally、finalize 的区别
> 关联：[java基础](../../java基础/)（关键字）

**final** 是关键字，可以修饰类、方法和变量。修饰类表示不能被继承，修饰方法表示不能被重写，修饰变量表示引用或值不能再次赋值。final 修饰对象引用时，只是引用不能变，对象内部状态仍然可能改变。

**finally** 是异常处理结构的一部分，通常用于释放资源。无论 try 中是否发生异常，只要 JVM 没有直接退出，finally 通常都会执行。

**finalize** 是 Object 类中的方法，在对象被 GC 回收前可能被调用，但它不可靠，也不推荐使用。现代 Java 里已经不建议依赖它做资源释放。资源释放应该用 try-with-resources。

### 3. try-catch-finally 的具体执行过程
> 关联：[java基础](../../java基础/)（异常处理）

try 中的代码先执行，如果没有异常，会跳过 catch，然后执行 finally。如果 try 中发生异常，会匹配对应的 catch，catch 执行完后再执行 finally。如果 try 或 catch 里有 return，finally 仍然会在真正返回前执行。

需要注意：如果 finally 里也写了 return，会覆盖 try 或 catch 里的返回值。如果 finally 修改的是局部变量，返回值是否变化要看返回的是基本类型值还是对象引用。

### 4. 线程怎么创建，生产里为什么不建议直接 new Thread
> 关联：[juc并发编程](../../juc并发编程/)（线程创建与线程池）

Java 创建线程可以继承 Thread、实现 Runnable、实现 Callable 配合 FutureTask，也可以使用线程池。生产环境里不建议大量直接 new Thread，因为线程创建和销毁成本高，而且没有统一的队列、限流、监控和拒绝策略。更推荐使用线程池，但也不建议直接用 Executors 的快捷方法，因为很多默认队列是无界的。更建议显式使用 ThreadPoolExecutor。

### 5. 怎么停止一个线程，为什么不推荐 stop
> 关联：[juc并发编程](../../juc并发编程/)（线程中断）

不推荐使用 Thread.stop()，因为它会强制终止线程，可能导致锁释放时对象处于不一致状态。更安全的方式是协作式停止：使用中断标记 interrupt、使用 volatile 标志位、使用线程池的 shutdown。

### 6. 线程池有哪些核心参数，怎么设置才不容易出问题
> 关联：[juc并发编程](../../juc并发编程/)（线程池参数）

线程池核心参数包括核心线程数、最大线程数、空闲线程存活时间、任务队列、线程工厂和拒绝策略。CPU 密集型线程数一般接近 CPU 核数；IO 密集型可以适当大一些。线上还要监控线程池状态。

### 7. 给一个合同异步审查场景，说一下线程池执行过程
> 关联：[juc并发编程](../../juc并发编程/)（线程池执行流程）

线程池接收到任务后，如果当前运行线程数小于核心线程数，就创建核心线程执行；如果核心线程已满，就进入阻塞队列；如果队列也满了，并且线程数没到最大线程数，就创建非核心线程；如果线程数也到最大值，就触发拒绝策略。如果任务很重，线程池只适合做本机调度，真正生产里更推荐用 MQ 或分布式任务系统削峰和解耦。

> 关联：[消息队列](../../消息队列/)（异步解耦）

### 8. SQL 中 group by、having 和子查询怎么口述清楚
> 关联：[mysql](../../mysql/)（SQL语法）

group by 用来分组，通常配合聚合函数使用。where 是分组前过滤明细行，having 是分组后过滤聚合结果。子查询是把一个查询结果作为另一个查询的条件或数据来源。

### 9. 数据库事务一致性怎么保障
> 关联：[mysql](../../mysql/)（事务ACID）

事务一致性不是只靠数据库自动保证，业务也要设计正确。数据库提供 ACID，其中原子性保证要么都成功要么都失败，隔离性保证并发事务之间互不干扰到一定程度，持久性保证提交后数据不会丢。一致性最终要靠约束、事务边界和业务逻辑共同保证。使用 @Transactional(rollbackFor = Exception.class) 保证事务回滚。

> 关联：[spring](../../spring/)（Spring事务管理）

*（剩余60%内容需付费查看，主要涉及AI应用开发相关问题）*

---

## 知识点模块汇总
- java基础：final/finally/finalize、异常处理
- juc并发编程：线程创建、线程中断、线程池参数、线程池执行流程
- mysql：SQL语法（group by/having/子查询）、事务ACID
- spring：Spring事务管理（@Transactional）
- 消息队列：异步解耦、MQ削峰
- Agent/AI：AI应用开发相关（付费内容部分）
