# 一、JWT 双令牌认证与权限体系

## 认证与鉴权

- 项目的认证模块是如何设计的？是否实现了鉴权？
- 双令牌相关
  - AccessToken 和 RefreshToken 分别用来解决什么问题，有什么好处，是否存在信息冗余？
  - 两种Token 中有什么内容？只用一个 Token（不用双令牌机制）行不行？
  - Token 是否需要加密？只签名不加密是否安全？如果采用加密方式保护 Token 内容，是否可以替代签名？
  - 为什么用 RS256 签名？RS256 和 HS256 有什么区别？为什么使用非对称签名，而不是对称签名？
  - AccessToken 使用什么进行加密，里面包含哪些信息，解析后后端会将其中的信息与哪些数据进行比对？采用加密（而非签名）来保护 Token可以吗？（如何计算和校验合法性）
  - 接口需要的 userId 是从 Token 拿到的，这个 userId 是每次请求都要从 Token 拿吗？有没有保存到 ThreadLocal 这样的用户上下文中？
  - 双令牌分别存储在哪 / Refresh Token 是否存储在 Redis 中）？为什么需要存储？
  - JWT 的校验机制是什么
  - JWT 一旦签发后无法主动失效，项目中如何解决这个问题？
  - Refresh Token 的续期流程是怎样的？用户使用 Refresh Token 换取新的 Access Token 时，后端会做哪些校验？
  - Redis 中的刷新令牌白名单是如何设计的？
  - 如何实现 Token 的即时撤销？用户退出登录后，Token 如何失效？

- 鉴权流程、信息获取相关
  - Spring Security 在项目中承担了哪些职责？认证和授权流程分别是怎样的？
  - 用户登录后，后端如何生成并返回 Token？
  - 接口请求中的用户身份信息是如何获取的？
  - Spring Security 中的 `@AuthenticationPrincipal` 是如何获取当前登录用户信息的？
  - Spring Security 的上下文是否基于 ThreadLocal？和手动使用 ThreadLocal 有什么区别？


### 其他相关：ThreadLocal

- ThreadLocal 的底层原理是什么？
- ThreadLocalMap 的 key 和 value 分别是什么？
- ThreadLocal 为什么可能造成内存泄漏？
- 在线程池环境下使用 ThreadLocal 有什么风险？
- 使用完 ThreadLocal 后为什么要调用 remove？
- Spring Security 是如何管理当前线程用户上下文的？
- 使用 Spring Security 上下文和手动维护 ThreadLocal 用户上下文有什么区别？

---

# 渐进式发布、OSS 直传与 DeepSeek 摘要生成

- 项目的渐进式发布流程是如何设计的？

- 草稿、待上传、已上传、待发布、已发布等状态是如何流转的？为什么要使用状态机管理文章发布流程？

- 如果文章发布过程中某一步失败，如何保证状态不会混乱？

- 创建草稿实例

  - 为什么发布内容时要先创建草稿记录？
  - 编辑文章时是否会保存草稿？草稿保存流程是怎样的？

- 预签名URL申请

  - 为什么需要后端生成 OSS 预签名 URL？
  - 预签名 URL 是如何返回给前端的？
  - 预签名 URL 通常包含哪些限制信息？过期后怎么办？
  - 用户上传过程中如果预签名 URL 失效，应该如何重新获取？

- 前端直传到 OSS、元数据完善、内容确认与校验，确认发布

  - 前端直传 OSS 相比后端中转上传有什么优势？
  - 前端直传 OSS 后，如何防止用户绕过后端直接伪造发布状态？
  - 为什么不让前端直接持有 OSS 的 AccessKey 和 SecretKey？
  - 前端拿到预签名 URL 后，是如何将文件上传到 OSS 的？
  - OSS 文件上传成功后，如何通知后端文件已经可用？后端如何补全文章的 OSS 信息？
  - 如果用户上传文件成功但未点击发布，系统如何处理这类临时资源？

  - 后端会做哪些校验？用户确认发布后，后端会更新哪些字段？


- DeepSeek 摘要生成功能

  - 项目中接入 DeepSeek 生成文章摘要是同步调用还是异步调用？

  - 如果摘要生成耗时较长，如何避免阻塞发布流程？

  - 摘要生成失败时，文章是否允许发布？

  - AI 摘要生成结果是否需要人工确认或二次编辑？

  - AI 摘要生成服务异常时，如何降级？

---

# Feed 流三级缓存、HotKey 探测与缓存一致性

## 3.1 Feed 流缓存架构设计

- 项目中的 Feed 流三级缓存是如何设计的？为什么要设计成本地 Caffeine 缓存、Redis 页面缓存、Redis 片段缓存三级结构？
- Caffeine 本地缓存、Redis 页面缓存、Redis 片段缓存都主要缓存哪些内容？
- 为什么要同时设计 Redis 页面缓存和 Redis 片段缓存？页面缓存和片段缓存各自解决什么问题？
- Feed 流缓存命中后，完整的数据返回流程是怎样的？
- 如果页面缓存未命中，但片段缓存命中，如何组装 Feed 页面？
- 如果三级缓存全部未命中，如何回源查询数据库？
- Feed 流缓存中的 key 是如何设计的？
- Redis 的 key 是否设置了 TTL？
- 不同类型 Feed 缓存的 TTL 是否相同？

## 数据一致性

- 项目中如何保证 Caffeine、Redis 和 MySQL 之间的数据一致性？
- 数据更新后，如何失效本地 Caffeine 缓存？多台应用节点中的 Caffeine 缓存如何同步失效？
- 如果 JVM 级缓存存储的是用户行为数据，而不是配置类数据，多台节点之间如何保证一致性？
- 本地缓存和分布式缓存之间是否可能出现短暂不一致？如何接受或修正？
- 缓存更新策略是先更新数据库再删除缓存，还是先删除缓存再更新数据库？
- 缓存删除失败会导致什么问题？如何补偿？
- Canal 订阅 Binlog 后如何驱动缓存失效？
- Kafka 消息延迟时，缓存是否会读到旧数据？
- 如何通过最终一致性保证 Feed 流数据不会长期错误？

## HotKey 探测与热点缓存保护

- 为什么需要 HotKey 探测？项目中的 HotKey 探测机制是如何设计的？多节点部署时，HotKey 探测结果如何汇总？
- HotKey 的统计数据存储在哪里？本地内存、Redis 还是其他组件？
- 如何判断某个 Feed 页面或文章成为热点？
- 热点页面的 TTL 为什么需要动态延长？
- HotKey 探测中滑动时间窗口是如何实现的？例如 60 秒窗口拆分为 6 个 10 秒分片时，环形数组和移动指针如何工作？
- HotKey 检测阈值如何设置？
- 如何防止热点 key 在同一时间大量过期？
  - 热点 key 的缓存过期时间是否需要随机抖动？
  - 热点 key 失效瞬间如何使用单飞锁防止缓存击穿？

- 如果某个热点 key 被频繁访问，如何避免请求直接打到数据库？

## 缓存穿透、击穿、雪崩

- 缓存穿透、缓存击穿、缓存雪崩分别是什么？
- 项目中是否遇到过缓存穿透？如何处理？
  - 除了缓存空值，还有哪些方式可以解决缓存穿透？
  - 如何在查询数据库之前判断某个数据是否一定不存在？
  - 布隆过滤器适合解决什么类型的缓存穿透问题？

- 缓存击穿和热点 key 有什么关系？
  - 如何通过互斥锁或单飞锁解决缓存击穿？

- 缓存雪崩通常由哪些原因造成？
  - 如何通过随机 TTL、分批预热、限流降级解决缓存雪崩？


## Feed 推拉模式与热点排序

- Feed 流采用推模式、拉模式还是推拉结合？
- 用户发布帖子后，他的粉丝如何收到这条内容？如果采用推模式，大 V 发布内容时会有什么问题？大 V 的 Feed 分发如何做特殊处理？
- 首页 Feed 的热度值是否通过加权公式计算？
  - Feed 热度值是否存入 Redis ZSet？
  - Feed 热度值的增量更新依据是什么？
  - Feed 热度排序如何结合点赞数、评论数、收藏数、时间衰减等因素？
  - 热度分数更新是实时计算还是异步批量计算？
  - Feed 缓存与热度 ZSet 如何配合？


---

# Outbox 事件驱动用户关系系统

## 关注关系数据模型

- 关注功能存储了哪些维度的数据？是否包含关注、粉丝、拉黑等关系类型？
- 表结构：following, follower的结构设计，为什么要设计两张表？分别存储有什么好处？
- following 表和 follower 表之间是否有一致性校验？
- 如果只存一张关注关系表，查询关注列表和粉丝列表会有什么问题？
- 表排序：
  - 是否使用 Redis ZSet 缓存？
  - 关注列表和粉丝列表如何做到有序？关注时间是否作为排序依据？
  - 是否支持按最近关注、共同关注、互动频率等维度排序？
- 一主多从是什么意思？流程是怎样的？

## 关注操作的业务链路

- 用户点击关注一次，会触发哪些业务逻辑？
- 关注操作是同步写数据库，还是异步写数据库？如果异步线程操作数据库发生异常，如何保证数据原子性？
- 关注关系的事实数据是否必须同步落库？链路中哪些数据要求强一致，哪些数据可以最终一致？
- 为什么关注关系相比点赞更强调强一致性？如何通过定时对账修复关注表和粉丝表不一致？关注关系的对账任务如何设计？
- 万级用户同时关注一个用户或主播时，如何处理并发写入？
- 重复关注、取消关注请求如何保证幂等？是否需要状态机设计？
  - 如果用户重复点赞，后端如何判断状态没有变化并直接返回？

- 频繁关注、取消关注如何处理事件乱序？是否需要通过版本号或时间戳解决关注事件乱序问题？
  - 如果用户连续点赞、取消、再点赞，如何保证最终状态正确？
  - 天天刷点赞接口的人如何限制？
  - 是否需要针对点赞接口做限流、风控或验证码？

- 关注数是单独计数累加，还是通过关注关系表实时统计？
- 关注关系写入后，粉丝表如何异步更新？关注列表、粉丝列表缓存如何更新或失效？

## Outbox 模式设计

- Outbox 模式是什么？项目中的 Outbox 表是如何设计的？Outbox 表中包含哪些字段？
- 关注表和 Outbox 表为什么要在同一个数据库事务中写入？
- 为什么用户关系模块使用 Outbox，而不是直接异步发送 Kafka 消息？
- 为什么不直接订阅关注表的 Binlog，而要额外写 Outbox 表？
- 如果没有 Outbox 设计，会出现哪些一致性问题？
- Outbox 如何降低业务写入和消息发送之间的耦合？
- Outbox 如何保证消息不会因为应用崩溃而丢失？
- 如果应用写入关注表成功，但发送 Kafka 消息失败，会发生什么？如果使用 Outbox，Kafka 暂时不可用时如何保证事件最终投递？
- Outbox 表中的事件何时标记为已发送？
- Outbox 表是否需要定期清理？

## Canal、Binlog 与 Kafka 投递

- Canal 订阅 MySQL Binlog 到 Kafka 的流程是怎样的？
- Canal 监听 Outbox 表时，会从 Binlog 中读取哪些关键信息？有哪些内容可以被 Canal 用来发送事件？
- Canal 和业务服务直接发送 Kafka 消息相比有什么区别？
- 如果 Kafka 失效，如何保证数据一致性？
- Canal 投递 Kafka 失败时如何处理？
- Kafka 消息重复投递时如何保证消费端幂等？
- Kafka 消息积压时，关注关系的数据一致性会受到什么影响？
- Canal 或 Kafka 延迟时，粉丝表、计数系统和缓存是否会短暂不一致？
- 如何通过最终一致性机制修正关注表和粉丝表的不一致？
- 消费失败如何进行重试？是否会进入死信队列？

---

# 点赞、收藏、位图判重与异步计数系统

## 点赞、收藏事实层设计

- 用户点赞业务的完整流程是什么？
- 请求进入后端后，Lua 脚本、Kafka、消费者、计数聚合分别做什么？
  - Kafka 收到点赞消息后会做什么？
- 前端点赞后的客户端展示逻辑是什么？点赞请求成功后，如果 Kafka 消息还没被消费，前端刷新页面点赞状态是否会消失？是否采用前端乐观更新点赞状态和计数？
- 多人同时点赞同一篇文章时，用户看到的点赞数如何展示？
- 点赞增量事件中包含哪些字段？
- 如何通过雪花算法生成唯一消息 ID？
- 用户点赞状态存储位置和记录是否落库
  - 点赞、收藏这类用户行为是否需要持久化？点赞状态只存在 Redis 中，如果 Redis 宕机会发生什么？点赞事实数据不落库是否合理？
  - 只依赖 Redis 主从复制能否保证完全不丢数据？Redis 主从切换期间是否可能丢失最近写入的数据？
  - Redis 集群、主从复制、哨兵或故障转移在点赞系统中解决什么问题？计数系统如何做灾难重建？计数重建的数据源来自哪里？
  - 如果要求 Redis 数据尽量不丢，是否需要开启 AOF always？代价是什么？
  - 点赞和关注为什么采用不同的数据一致性策略？点赞业务为什么更偏向高性能和最终一致性？关注业务为什么更偏向强一致和可追溯？
  - 同一个用户对同一篇文章的点赞和取消点赞事件如何保证有序消费？（加版本号或时间戳）

- Redis Bitmap
  - 点赞状态为什么使用 Redis Bitmap 存储？
  - Bitmap 中存储的内容是什么？
  - 一个用户的点赞状态如何映射到 Bitmap 的某一位？
  - 用户 ID 如何计算出所在的分片和 bit 位偏移？
  - 一个 Bitmap 分片可以支持多少用户状态存储？
  - 分片位图的分片大小是多少？分片大小为 32K 时，一个分片的 Bitmap 大约占用多少内存？
  - 为什么要做 Bitmap 分片，而不是所有用户共用一个大 Bitmap？

- 拓展：如果只用 Bitmap 存储点赞状态，如何支持查看点赞用户列表？
  - 如果要展示某篇文章的点赞列表，如何设计数据结构？(引入 Redis ZSet？score 应该使用什么，Bitmap 和 ZSet 同时写入时如何保证一致性？写成功失败，Lua脚本)
  - 按点赞时间排序和按是否关注排序分别如何实现？使用多个 ZSet 和查询时过滤各有什么优缺点？
  - 如果要根据点赞数做文章排行榜，如何使用 ZSet 设计？点赞排行榜的 score 如何更新？点赞排行榜是实时更新还是异步批量更新？


## Lua 脚本与原子判重

- 点赞功能中的 Lua 脚本具体用在哪里？为什么需要使用 Lua 脚本？（Check-Then-Act 竞态问题）
- Lua 脚本如何保证“判断状态 + 修改状态 + 返回结果”的原子性？
- 多个 Redis 操作在一个 Service 中执行时，如何保证原子性？
- 除了 Lua 脚本，还有哪些方式可以保证 Redis 多操作原子性？
- Redis 事务和 Lua 脚本有什么区别？
- Redis key 是否设置 TTL？点赞状态 key 是否应该过期？

## 汇总计数存储与聚合

- 点赞数、收藏数、评论数等汇总计数存储在哪里？
- 本地聚合窗口如何统计一段时间内的点赞增量？计数系统如何进行增量聚合？本地聚合后如何批量刷写到 Redis？
- 多台消费者本地聚合时，如何保证同一个实体的增量不会被分散到多个机器导致统计混乱？
- 如果同一篇文章的点赞事件被分散到多个分区，如何汇总？
- 如何识别哪些 Kafka 分区负载较高？如何定位高负载分区是由哪些热点文章导致的？热点文章导致 Kafka 分区负载不均衡时如何处理？
- Redis 实现计数系统使用了什么数据类型？为什么选择自定义 Redis SDS，而不是 Redis Hash？为什么不直接把所有计数都存储在 Redis String 或 Hash 中？
- SDS 存储计数有什么优势、缺点？
- 是否可以直接把最终计数存在 Redis SDS 中？

# 建议重点备的高频主线

如果时间有限，建议你优先把下面几条主线讲清楚，因为它们在面经里反复出现：

## 点赞计数主线

需要能完整讲清：

- Bitmap 如何存用户点赞状态；
- Lua 如何保证判重和状态修改原子性；
- Kafka 如何传递点赞增量事件；
- 消费端如何做幂等、乱序处理和本地聚合；
- Redis 中如何存储最终计数；
- Redis 宕机后如何重建、对账和降级；
- 如果要支持点赞列表或排行榜，如何引入 ZSet。

## 关注 Outbox 主线

需要能完整讲清：

- 为什么关注比点赞更需要强一致；
- following/follower 两张表为什么都需要；
- Outbox 表为什么和关注表同事务写入；
- Canal 如何订阅 Binlog 并投递 Kafka；
- 消费端如何更新粉丝表、计数和缓存；
- 消息丢失、重复、延迟时如何保证最终一致。

## Feed 缓存主线

需要能完整讲清：

- Caffeine、Redis 页面缓存、Redis 片段缓存分别缓存什么；
- 为什么要页面缓存和片段缓存并存；
- 缓存一致性如何通过 Binlog/Kafka/删除缓存保证；
- HotKey 如何探测；
- 热点页面 TTL 如何动态延长；
- 如何防止穿透、击穿、雪崩。

## 双 Token 主线

需要能完整讲清：

- Access Token 和 Refresh Token 的职责差异；
- 为什么短期 Access Token + 长期 Refresh Token 更安全；
- Refresh Token 为什么要进 Redis 白名单；
- RS256 为什么适合服务端签发、多端校验；
- 如何续期、撤销和处理 Token 泄露。

## 渐进式发布主线

需要能完整讲清：

- 为什么先创建草稿记录；
- 为什么前端直传 OSS；
- 后端预签名如何保证安全；
- OSS 上传成功后如何补全状态；
- DeepSeek 摘要生成如何异步化和降级。