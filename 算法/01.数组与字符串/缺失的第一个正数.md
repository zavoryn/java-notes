---
tags: [算法/数组, leetcode/hot100, ACM模式]
difficulty: 困难
leetcode: 41
---

# 缺失的第一个正数

> [LeetCode 41. First Missing Positive](https://leetcode.cn/problems/first-missing-positive/)

## 题目

给你一个未排序的整数数组 nums，请你找出其中没有出现的最小的正整数。O(n) 时间 + O(1) 空间。

```
输入：nums = [3,4,-1,1]
输出：2
```

## 解答

```java
public static int firstMissingPositive(int[] nums) {
    int n = nums.length;
    for (int i = 0; i < n; i++)
        while (nums[i] > 0 && nums[i] <= n && nums[nums[i] - 1] != nums[i])
            swap(nums, i, nums[i] - 1);
    for (int i = 0; i < n; i++)
        if (nums[i] != i + 1) return i + 1;
    return n + 1;
}
```

> 原地哈希：把值 x 放到下标 x-1 处。最终第一个 `nums[i] != i+1` 处就是答案。
