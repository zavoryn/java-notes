---
tags:
  - 算法/栈
  - leetcode/hot100
  - ACM模式
difficulty: 简单
leetcode: 20
---

# 有效的括号

> [LeetCode 20. Valid Parentheses](https://leetcode.cn/problems/valid-parentheses/) ｜ [牛客 NC52 ACM 练习](https://www.nowcoder.com/practice/37548e94a270412c8b9fb85643c8ccc2)

## 题目

给定一个只包括 `(`、`)`、`{`、`}`、`[`、`]` 的字符串 `s`，判断字符串是否有效。

有效字符串需满足：左括号必须用相同类型的右括号闭合，左括号必须以正确的顺序闭合。

```
输入：s = "()[]{}"
输出：true

输入：s = "([)]"
输出：false
```

## ACM 输入格式

```
一行：括号字符串
```

```
()[]{}
```

## 解答

```java
import java.util.*;

public class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        String s = sc.nextLine();
        System.out.println(isValid(s));
        sc.close();
    }

    public static boolean isValid(String s) {
        Deque<Character> stack = new ArrayDeque<>();
        for (char c : s.toCharArray()) {
            if (c == '(' || c == '[' || c == '{') {
                stack.push(c);
            } else {
                if (stack.isEmpty()) return false;
                char top = stack.pop();
                if (c == ')' && top != '(') return false;
                if (c == ']' && top != '[') return false;
                if (c == '}' && top != '{') return false;
            }
        }
        return stack.isEmpty();
    }
}
```

## 复杂度

- **时间**：O(n)
- **空间**：O(n)

---

## 踩坑记录

> ❓ 用 `Stack` 还是 `Deque`？
>
> `Stack` 是遗留类，继承 `Vector`，方法加了 `synchronized`，性能差。推荐用 `Deque<Character> stack = new ArrayDeque<>()`，`push` / `pop` / `peek` 三件套不变。
>
> ⚠️ **手写提醒**：`new ArrayDeque<>()` 这个构造容易忘，记住 `Deque<Character> stack = new ArrayDeque<>()`。

## 关联题目

- [[最小栈]] — 栈变体，多维护一个最小值
- [[05.栈/每日温度|每日温度]] — 单调栈思想
