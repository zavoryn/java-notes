---
tags: [算法/二分查找, leetcode/hot100, ACM模式]
difficulty: 中等
leetcode: 74
---

# 搜索二维矩阵

> [LeetCode 74. Search a 2D Matrix](https://leetcode.cn/problems/search-a-2d-matrix/)

## 题目

给你一个满足下述两条属性的 m x n 整数矩阵：每行中的整数从左到右按非严格递增顺序排列；每行的第一个整数大于前一行的最后一个整数。给你一个整数 target，如果 target 在矩阵中，返回 true；否则返回 false。

```
输入：matrix = [[1,3,5,7],[10,11,16,20],[23,30,34,60]], target = 3
输出：true
```

## 解答

```java
public boolean searchMatrix(int[][] matrix, int target) {
    int m = matrix.length, n = matrix[0].length;
    int left = 0, right = m * n - 1;
    while (left <= right) {
        int mid = left + (right - left) / 2;
        int val = matrix[mid / n][mid % n];
        if (val == target) return true;
        else if (val < target) left = mid + 1;
        else right = mid - 1;
    }
    return false;
}
```

## 复杂度

- **时间**：O(log(m×n))
- **空间**：O(1)

## 复习标记

> 🔁 2026-05-18 和搜索二维矩阵II混淆——LC74行首>上行行尾，整体可当一维有序数组二分，mid映射：`row=mid/n, col=mid%n`
