---
tags:
  - 算法/回溯
  - leetcode/hot100
  - ACM模式
difficulty: 中等
leetcode: 78
---

# 子集

> [LeetCode 78. Subsets](https://leetcode.cn/problems/subsets/) ｜ [牛客 NC27](https://www.nowcoder.com/practice/c333d551472f4bb3825f1f5b3f5b3f5b)

## 题目

给你一个整数数组 `nums`，数组中的元素**互不相同**。返回该数组所有可能的子集（幂集）。

```
输入：nums = [1,2,3]
输出：[[],[1],[2],[1,2],[3],[1,3],[2,3],[1,2,3]]
```

## ACM 输入格式

```
第一行：n
第二行：n 个整数
```

```
3
1 2 3
```

## 解答

```java
import java.util.*;

public class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int n = sc.nextInt();
        int[] nums = new int[n];
        for (int i = 0; i < n; i++) nums[i] = sc.nextInt();

        List<List<Integer>> res = subsets(nums);
        System.out.println(res);
        sc.close();
    }

    public static List<List<Integer>> subsets(int[] nums) {
        List<List<Integer>> res = new ArrayList<>();
        backtrack(nums, 0, new ArrayList<>(), res);
        return res;
    }

    static void backtrack(int[] nums, int start, List<Integer> path, List<List<Integer>> res) {
        res.add(new ArrayList<>(path)); // 每个节点都收集
        for (int i = start; i < nums.length; i++) {
            path.add(nums[i]);
            backtrack(nums, i + 1, path, res); // i+1：不可重复选
            path.remove(path.size() - 1);
        }
        
    }
}
```

## 复杂度

- **时间**：O(n × 2^n)
- **空间**：O(n)

---

## 三题对比：回溯模板参数差异

| 题 | 递归 start | used | 收集时机 |
|------|------|------|------|
| 全排列 | 无（每次从0开始） | `boolean[]` | `path.size() == n` |
| 组合总和 | `i`（可重复） | 无 | `sum == target` |
| 子集 | `i+1`（不可重复） | 无 | **每个节点都收集** |

## 关联题目

- [[全排列]] — 回溯模板参数差异
- [[组合总和]] — 同类 start 控制思想
