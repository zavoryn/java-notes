---
tags: [算法/矩阵, leetcode/hot100, ACM模式]
difficulty: 中等
leetcode: 73
---

# 矩阵置零

> [LeetCode 73. Set Matrix Zeroes](https://leetcode.cn/problems/set-matrix-zeroes/)

## 题目

给定一个 m x n 矩阵，如果一个元素为 0，则将其所在行和列的所有元素都设为 0。请原地实现。

## 解答

```java
public static void setZeroes(int[][] matrix) {
    int m = matrix.length, n = matrix[0].length;
    boolean row0 = false, col0 = false;
    for (int i = 0; i < m; i++) if (matrix[i][0] == 0) col0 = true;
    for (int j = 0; j < n; j++) if (matrix[0][j] == 0) row0 = true;
    for (int i = 1; i < m; i++)
        for (int j = 1; j < n; j++)
            if (matrix[i][j] == 0) { matrix[i][0] = 0; matrix[0][j] = 0; }
    for (int i = 1; i < m; i++)
        for (int j = 1; j < n; j++)
            if (matrix[i][0] == 0 || matrix[0][j] == 0) matrix[i][j] = 0;
    if (row0) for (int j = 0; j < n; j++) matrix[0][j] = 0;
    if (col0) for (int i = 0; i < m; i++) matrix[i][0] = 0;
}
```

> 首行首列做标记位。

## 复习标记

> 🔁 2026-05-13 思路遗忘——首行首列当标记数组：第一遍正向扫描打标记，第二遍倒序置零（避免破坏标记），最后用 col0 处理首列本身
