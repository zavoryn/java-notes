---
tags: [算法/位运算, leetcode/hot100, ACM模式]
difficulty: 简单
leetcode: 169
---

# 多数元素

> [LeetCode 169. Majority Element](https://leetcode.cn/problems/majority-element/)

## 题目

给定一个大小为 n 的数组，返回其中的多数元素。多数元素是指在数组中出现次数大于 ⌊n/2⌋ 的元素。

```
输入：nums = [3,2,3]
输出：3
```

## 解答

```java
public static int majorityElement(int[] nums) {
    int candidate = 0, count = 0;
    for (int x : nums) {
        if (count == 0) candidate = x;
        count += (x == candidate) ? 1 : -1;
    }
    return candidate;
}
```

> Boyer-Moore 投票算法：多数元素 > n/2，投票最终必然它胜出。
