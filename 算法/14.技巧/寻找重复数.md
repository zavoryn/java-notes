---
tags: [算法/双指针, leetcode/hot100, ACM模式]
difficulty: 中等
leetcode: 287
---

# 寻找重复数

> [LeetCode 287. Find the Duplicate Number](https://leetcode.cn/problems/find-the-duplicate-number/)

## 题目

给定一个包含 n+1 个整数的数组 nums，其数字都在 [1,n] 范围内。nums 中有且只有一个重复的整数，返回这个重复的数。要求不修改数组且只用常量级 O(1) 额外空间。

## 解答

```java
public static int findDuplicate(int[] nums) {
    int slow = nums[0], fast = nums[nums[0]];
    while (slow != fast) { slow = nums[slow]; fast = nums[nums[fast]]; }
    slow = 0;
    while (slow != fast) { slow = nums[slow]; fast = nums[fast]; }
    return slow;
}
```

> 数组看作链表：`i -> nums[i]`，重复数导致环入口。Floyd 判圈找入口。
